// Specializations shown as wide cards in <Services />. Text (title,
// description, stat) comes from dictionary[lang].services.items in this
// same order — only the icon is configured here.

export const serviceIcons = ['Code2', 'Server', 'Layers']
